<?php
if (!isset($_SESSION)){
    session_start();
}
// session_unset();
// $_SESSION['uploaded_images'] = [];
// session_unset();
// if(pageName() == "products")
// dd(password_hash("12345678", PASSWORD_BCRYPT));